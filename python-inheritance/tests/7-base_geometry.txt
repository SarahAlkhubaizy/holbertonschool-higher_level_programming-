===============================
How to use 7-base_geometry.py
===============================

First, import the BaseGeometry class:
    >>> BaseGeometry = __import__('7-base_geometry').BaseGeometry

Create an instance of BaseGeometry:
    >>> bg = BaseGeometry()

Testing area() method, it should raise an Exception:
    >>> try:
    ...     bg.area()
    ... except Exception as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [Exception] area() is not implemented

Testing integer_validator() with a valid integer:
    >>> bg.integer_validator("my_int", 12)

Testing integer_validator() with a 0 (should raise ValueError):
    >>> try:
    ...     bg.integer_validator("width", 0)
    ... except ValueError as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [ValueError] width must be greater than 0

Testing integer_validator() with a negative number (should raise ValueError):
    >>> try:
    ...     bg.integer_validator("height", -10)
    ... except ValueError as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [ValueError] height must be greater than 0

Testing integer_validator() with a string (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("name", "John")
    ... except TypeError as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [TypeError] name must be an integer

Testing integer_validator() with a float (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("weight", 12.5)
    ... except TypeError as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [TypeError] weight must be an integer